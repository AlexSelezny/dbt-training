version: 2

models:
  - name: stg_orders
    descriptopm: one unique order per row
    columns:
      - name: orderid
        description: the primary key for stg_orders
      - name: shipmode
        description: '{{doc("shipmode")}}'